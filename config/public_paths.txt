# Only paths listed here are publicly accessible.
# Avoid exposing the root path (/), as it often pulls in additional
# resources (CSS, images, fonts) that would also need to be exposed.
# Default: all paths are private.
# /
/foo
/bar
/favicon.ico
/robots.txt
/humans.txt
/site.webmanifest
/.well-known/appspecific/com.chrome.devtools.json
