name: Fallow

on:
  push:
    branches: [ master, production ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  fallow:
    name: Codebase Intelligence
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Fallow - Codebase Intelligence
        uses: fallow-rs/fallow@v2.58.0
